using Architecture;

public class TooltipUIController : UIController
{
    /*public TooltipUI tooltip;

    public override void OnCreate()
    {
        
        var tooltip = Object.FindObjectOfType<TooltipUI>();
        if (tooltip == null)
        {
            FindCanvas();
            TooltipUI uiLoad = Resources.Load<TooltipUI>("UITooltip");
            this.tooltip = Object.Instantiate(uiLoad, uiContainer.transform);
        }
        else
            this.tooltip = tooltip;
    }*/
}
