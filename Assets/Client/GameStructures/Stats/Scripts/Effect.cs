using UnityEngine;

namespace GameStructures.Stats
{
  /*  public abstract class Effect : BaseStat
    { 
        protected EffectPreset effectPreset;
        public EffectType EffectType => effectPreset.EffectType;

        public override void Initialize(StatsHandler handler)
        {

            base.Initialize(handler);

            if (statPreset is null)
                statPreset = effectPreset;
            else
                effectPreset = statPreset as EffectPreset;
        }
    }
    public enum EffectType
    {
        Multiplier,
        Dot,
    }*/
}
